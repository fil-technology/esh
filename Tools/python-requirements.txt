mlx>=0.31.2
mlx-lm>=0.31.3
mlx-vlm==0.5.0
safetensors>=0.4,<1
# On-device speech: STT (parakeet) for voice transcription and TTS playback. Without this the voice
# loop and /v1/audio/* endpoints report "speech runtime not installed" instead of transcribing.
mlx-audio>=0.5.1
# OPTIONAL (UCMR 2.1): background removal / segmentation (image.segment / image.edit). Without these the
# capability returns a clear "rembg is not available" error instead of segmenting. Heavy (onnxruntime).
# rembg
# onnxruntime
# OPTIONAL (UCMR 2.1 Stage 3): text->image + upscale via mflux (image.generate, and the experimental
# SeedVR2 image.upscale backend). Without it these return a clear "mflux is not available" error.
# mflux
# OPTIONAL (UCMR 2.1 Stage 4.1): image.upscale default backend — Real-ESRGAN ONNX on onnxruntime (CoreML
# EP), torch-free. Needs onnxruntime + pillow + huggingface_hub (model auto-downloaded to the assets root).
# onnxruntime
# pillow
# huggingface_hub
# OPTIONAL (UCMR 2.1 Stage 3): speaker diarization (audio.diarize) via sherpa-onnx (onnxruntime).
# sherpa-onnx
# soundfile
